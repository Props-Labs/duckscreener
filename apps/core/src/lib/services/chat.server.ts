import { env } from '$env/dynamic/private';
import { getValue, storeValue } from './redis';


export const getChatData = async (pool_id: string, offset: number = 0, limit: number = 1000) => {
    
}

export const addChatMessage = async (pool_id: string, account: string, message: string) => {
    
}
